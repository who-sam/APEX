VITE_API_URL=/api

# Google OAuth (Identity Services). Web client ID from Google Cloud Console.
# Must match the GOOGLE_CLIENT_ID set on the Go backend.
VITE_GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=apex
JWT_SECRET=dev-secret-change-in-prod
JUDGE0_URL=https://ce.judge0.com

# Password reset emails. Leave SMTP_HOST empty in dev — link logged to console.
APP_URL=http://localhost:5173
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Google OAuth (Identity Services). Web client ID from Google Cloud Console.
GOOGLE_CLIENT_ID=

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=apex
JWT_SECRET=dev-secret-change-in-prod
JUDGE0_URL=https://ce.judge0.com

# Email notifications. Leave SMTP_HOST empty in dev — links logged to console.
# For Gmail: enable 2FA then create an App Password at myaccount.google.com/apppasswords
APP_URL=http://localhost:5173
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM=your-email@gmail.com

# Google OAuth (Identity Services). Web client ID from Google Cloud Console.
GOOGLE_CLIENT_ID=
